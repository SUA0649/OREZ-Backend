services:
  - type: web
    name: orez-backend
    plan: free
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: orez-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: orez-redis
          property: connectionString

  - type: redis
    name: orez-redis
    plan: free
    ipAllowList: []

databases:
  - name: orez-db
    databaseName: appdb
    user: orez
    plan: free
