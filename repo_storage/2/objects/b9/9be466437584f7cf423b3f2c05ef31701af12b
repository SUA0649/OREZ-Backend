#include <iostream>
#include <fstream>
#include <string>
#include <cstdlib>
#include <ctime>
using namespace std;

string generateBigInt(int digits) {
    string s;
    s += (rand() % 9 + 1) + '0';
    for (int i = 1; i < digits; i++)
        s += (rand() % 10) + '0';
    return s;
}

int main() {
    srand(time(0));
    for (int f = 1; f <= 10; f++) {
        string filename = "input" + to_string(f) + ".txt";
        ofstream out(filename);
        string num1 = generateBigInt(100 + rand() % 50);
        string num2 = generateBigInt(100 + rand() % 50);
        out << num1 << "\n" << num2 << "\n";
        out.close();
        cout << "Generated " << filename << endl;
    }
}